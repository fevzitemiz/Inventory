import { Routes } from '@angular/router';
import { InventoryRecordsComponent } from './inventory-records/inventory-records.component';

export const INVENTORYRECORD_ROUTES: Routes = [
    { path: '', component: InventoryRecordsComponent },
];